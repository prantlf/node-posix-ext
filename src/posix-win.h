#ifndef POSIX_WIN_H
#define POSIX_WIN_H

#include <nan.h>

namespace posix_win {

// to be called during the node add-on initialization
NAN_MODULE_INIT(init);

} // namespace posix_win

#endif // POSIX_WIN_H
